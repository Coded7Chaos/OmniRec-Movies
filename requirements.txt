# =============================================================================
# Pipeline ML / notebooks 01–03
# =============================================================================
# NumPy fijado a la serie 1.26 porque scikit-surprise==1.1.4 se distribuye
# como wheel compilado contra NumPy 1.x. Con NumPy 2.x aparece el error
# "numpy.core.multiarray failed to import" al hacer `from surprise import ...`.
# Polars 1.9, pandas 2.2 y scikit-learn 1.5 son compatibles con NumPy 1.26.
numpy>=1.26,<2.0
polars==1.9.*
pyarrow==18.*
pandas==2.2.*
scikit-learn==1.5.*
scikit-surprise==1.1.4
psutil==6.1.*
matplotlib==3.9.*
seaborn==0.13.*
jupyter
ipykernel
tqdm
wordcloud
joblib
# Opcional — AutoML benchmark con TPE (si la instalación falla, el notebook
# 03 detecta ImportError y usa un GridSearchCV multi-algoritmo como fallback).
# auto-surprise==0.1.8

# =============================================================================
# Backend FastAPI (CRISP-DM Fase 6 — Deployment) en `app/backend/`
# =============================================================================
# API REST que sirve los modelos entrenados (baseline bayesiano + SVD con
# folding-in) a la aplicación web Next.js de `app/web-app/`.
# Espejo de `app/backend/requirements.txt`.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pyjwt>=2.9
bcrypt>=4.2
pydantic[email]>=2.9

# =============================================================================
# Fase 4 — MLOps + RAG / recuperación semántica (src/ + pipeline)
# =============================================================================
# Encoder de embeddings (preentrenado, inferencia, CPU), índice vectorial,
# tracking de experimentos, configuración y capa generativa.
# Nota: torch se fija a 2.4.* porque wheels de torch más nuevos fallan al cargar
# en algunos Windows (WinError 1114 en c10.dll); 2.4 es estable con numpy 1.26.
torch==2.4.*
sentence-transformers>=3.0
faiss-cpu>=1.8
mlflow>=2.16
pyyaml>=6.0
anthropic>=0.40        # capa RAG generativa (opcional; fallback por plantilla)
